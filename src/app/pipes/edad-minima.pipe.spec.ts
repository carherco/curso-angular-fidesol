import { EdadMinimaPipe } from './edad-minima.pipe';

describe('EdadMinimaPipe', () => {
  it('create an instance', () => {
    const pipe = new EdadMinimaPipe();
    expect(pipe).toBeTruthy();
  });
});
